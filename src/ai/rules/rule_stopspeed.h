#ifndef RULE_STOPSPEED_H
#define RULE_STOPSPEED_H

#include <QObject>

class Rule_StopSpeed : public QObject
{
    Q_OBJECT
public:
    explicit Rule_StopSpeed(QObject *parent = 0);

signals:

public slots:
};

#endif // RULE_STOPSPEED_H
